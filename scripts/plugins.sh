# Plugins list
PLUGINS="vagrant-bundler vagrant-cachier vagrant-mongodb vagrant-mysql vagrant-node vagrant-proxy vagrant-rake vagrant-rsync vagrant-vbguest"

# Install plugins
vagrant plugin install $PLUGINS
